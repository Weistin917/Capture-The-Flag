# Capture The Flag
## Build and run
If first time building, then inside the project root directory:
```
mkdir build && cd build
cmake ..
make
```
Otherwise, run inside `build`:
```
make
```
Run it: (inside `build`)
```
./ctf server -name "KDJ's Company" -p 4951
./ctf client
```